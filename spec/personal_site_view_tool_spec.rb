require "spec_helper"

describe PersonalSiteViewTool do
  it "has a version number" do
    expect(PersonalSiteViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
